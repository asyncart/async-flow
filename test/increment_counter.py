# setup python
# setup init + deploy functions
# setup send transaction function 
# hit increment counter transaction and do this to check:
# assert the transaction succeeded/failed as expected 
# assert that events that are expected to be emitted are emitted (if possible)
# assert that count after is as expected